package pages

import (
	"github.com/ansufw/gomono/views/layouts"
	"github.com/ansufw/gomono/internal/apps/web/data"
	"fmt"
)

templ Home() {

	
	@layouts.Base("home") {
		
		<h1>this is from home.templ wefe</h1>
		<p>delme API URL: { fmt.Sprintf("%s/%s", data.GetAPIBaseURL(ctx, "template-data"), "login" ) }</p>

		
	}
}